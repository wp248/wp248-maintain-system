# directories
node_modules/
.sass-cache/
log/
vendor/
local-site/

# build outputs
build/
*.log
*.map

# build tools lock
composer.lock
package-lock.min.json
yarn.lock

# IDE's
.project/
.vscode/
.idea/

# other
Thumbs.db
*.DS_Store
coverage/

# Ignoge local .env
.env
.env.local
.env.staging
git rm .env.local --cached
git rm .env.staging --cached
git rm .env --cached
git commit -m "Stopped tracking .en env.local, and env.staging"
